services:
  postgres:
    image: postgres:18
    container_name: medical_records_db
    environment:
      POSTGRES_DB: medical-records
      POSTGRES_USER: Josip
      POSTGRES_PASSWORD: Pa55w.rd
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U Josip -d medical-records"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
